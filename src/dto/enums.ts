export enum EntityStatus {
  ACTIVE = 'activo',
  INACTIVE = 'inactivo',
}

export enum Role {
  ADMIN = 'admin',
  EMPLOYEE = 'empleado',
  CLIENT = 'cliente',
}

export enum OrderStatus {
  PENDING = 'pendiente',
  PROCESSED = 'procesada',
  ANULATED = 'anulado',
  CANCELED = 'cancelado',
}
